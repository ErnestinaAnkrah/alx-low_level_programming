#include<stdio.h>
/**
 * main - Entry point
 * Description: 'prints the alphabet in lowercase, and then in uppercase'
 * Return: 0 (Success)
 */
int main(void)
{
	putchar(65);
	putchar(');
	return (0);
}
